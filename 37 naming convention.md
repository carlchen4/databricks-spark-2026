你这个问得非常好，而且是 **Spark / Databricks 里“默认约定但没人明说”的点**。

---

## `_ts` 是什么？

👉 **`_ts` 不是 Spark 的关键字，也不是系统字段**
👉 **它只是一个“命名习惯（convention）”**

---

## 在你前面那些例子里，`txn_ts` / `_ts` 表示什么？

通常：

```text
_ts  = timestamp
txn_ts = transaction timestamp
```

也就是：

> **一笔交易真正发生的时间**

---

## 银行 / 数据工程里常见的时间字段命名

| 字段名                | 含义                 |
| ------------------ | ------------------ |
| `txn_ts`           | 交易发生时间（event time） |
| `transaction_date` | 交易日期（可能没时分秒）       |
| `created_ts`       | 系统写入时间             |
| `updated_ts`       | 更新时间               |
| `_ts`              | 简写，通常是 timestamp   |

---
